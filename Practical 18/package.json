{
  "name": "creative-task-manager",
  "version": "1.0.0",
  "description": "A beautiful MERN stack task management application",
  "main": "backend/server.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && node server.js",
    "client": "cd frontend && npm start",
    "install-deps": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "build": "cd frontend && npm run build"
  },
  "keywords": ["mern", "react", "node", "express", "mongodb", "task-manager"],
  "author": "PAVAN DEVMURARI",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}